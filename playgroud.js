export let newMessage = "say hi";

// In 'playgroud.js'
// export default someValue; // Replace someValue with what you want to export as the default.

export function setText(txt) {
    newMessage = txt;
}